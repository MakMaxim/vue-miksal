export default {
   ISBURGERMENUOPEN(state) {
      return state.isBurgerMenuOpen;
   },
   ISBURGERPUSH(state) {
      return state.isBurgerPush;
   },
   ISORDERFORMOPEN(state) {
      return state.isOrderFormOpen;
   },
   ISSUCCESSFORMOPEN(state) {
      return state.isSuccesFormOpen;
   },
   WIDTH(state) {
      return state.width;
   },
   HEIGHT(state) {
      return state.height;
   },
   MATERIALS(state) {
      return state.materials;
   },
   SCHEMES(state) {
      return state.schemes;
   },
};
